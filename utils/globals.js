const global = window;
const {
  document,
  requestAnimationFrame,
  Math,
  cancelAnimationFrame,
  setTimeout,
} = window;

export const globals = {
  document,
  requestAnimationFrame,
  Math,
  cancelAnimationFrame,
  global,
  setTimeout,
};
